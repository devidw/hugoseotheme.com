{
  "devDependencies": {
    "node-sass": "^6.0.1",
    "nodemon": "^2.0.12"
  },
  "scripts": {
    "build-css": "node-sass --include-path scss ./src/import.sass ./src/import.min.css --recursive",
    "watch-css": "nodemon -e scss -x 'npm run build-css'"
  }
}
